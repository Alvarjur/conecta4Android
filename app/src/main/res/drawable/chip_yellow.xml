<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Color de fondo -->
    <solid android:color="#FF0" />

    <!-- Borde (opcional) -->
    <stroke
        android:width="2dp"
        android:color="@color/white" />

    <!-- Tamaño fijo (opcional, puedes usar wrap_content en el layout) -->
    <size
        android:width="48dp"
        android:height="48dp" />
</shape>